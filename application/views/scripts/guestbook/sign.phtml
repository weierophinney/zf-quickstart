Please use the form below to sign our guestbook!

<?php
$this->vars('form')->setAction($this->plugin('url')->direct(
    array('controller' => 'guestbook', 'action' => 'sign'),
    array('name' => 'default')
));
$form = $this->vars('form');
echo $form->render($this);
