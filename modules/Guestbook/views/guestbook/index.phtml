<?php $vars = $this->vars(); ?>
<p><a href="<?php echo $this->plugin('url')->direct(
    array(
        'controller' => 'guestbook',
        'action'     => 'sign'
    ),
    array('name' => 'default')
) ?>">Sign Our Guestbook</a></p>

Guestbook Entries: <br />
<dl>
    <?php foreach ($this->vars('entries') as $entry): ?>
    <dt><?php echo $vars->escape($entry->email) ?></dt>
    <dd><?php echo $vars->escape($entry->comment) ?></dd>
    <?php endforeach ?>
</dl>
