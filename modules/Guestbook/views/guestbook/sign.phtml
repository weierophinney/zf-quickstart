Please use the form below to sign our guestbook!

<?php
$form = $this->form;
$form->setAction($this->url(array('controller' => 'guestbook', 'action' => 'sign'), array('name' => 'default')))
     ->setView($this);
echo $form;
