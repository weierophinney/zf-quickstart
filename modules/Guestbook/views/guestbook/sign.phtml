Please use the form below to sign our guestbook!

<?php
$form = $this->vars('form');
$form->setAction($this->plugin('url')->direct(array('controller' => 'guestbook', 'action' => 'sign'), array('name' => 'default')))
     ->setView($this);
echo $form;
